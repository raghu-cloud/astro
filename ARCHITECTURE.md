# AWS App Runner Deployment Architecture

## 🏗️ Current Architecture (Recommended: Docker-Based)

```
┌─────────────────────────────────────────────────────────────────┐
│                         Developer                                │
│                                                                   │
│  ┌──────────────┐    ┌──────────────┐    ┌─────────────────┐   │
│  │   Code       │───▶│   Docker     │───▶│  Push to ECR    │   │
│  │   Changes    │    │   Build      │    │                 │   │
│  └──────────────┘    └──────────────┘    └─────────────────┘   │
│                                                     │             │
└─────────────────────────────────────────────────────┼───────────┘
                                                      │
                                                      ▼
                          ┌────────────────────────────────────────┐
                          │      Amazon ECR                        │
                          │  (Elastic Container Registry)          │
                          │                                        │
                          │  ┌──────────────────────────────────┐ │
                          │  │  astro-ai:latest                 │ │
                          │  │  (Docker Image)                  │ │
                          │  └──────────────────────────────────┘ │
                          └────────────────┬───────────────────────┘
                                           │
                                           │ Pull Image
                                           ▼
                          ┌────────────────────────────────────────┐
                          │      AWS App Runner                    │
                          │                                        │
                          │  ┌──────────────────────────────────┐ │
                          │  │  Service: astro-ai-service       │ │
                          │  │  ─────────────────────────────   │ │
                          │  │  • CPU: 2 vCPU                   │ │
                          │  │  • Memory: 4 GB                  │ │
                          │  │  • Port: 8000                    │ │
                          │  │  • Auto-scaling: Enabled         │ │
                          │  │                                  │ │
                          │  │  Running:                        │ │
                          │  │  ├─ Django App                   │ │
                          │  │  ├─ Gunicorn (2 workers)         │ │
                          │  │  ├─ FFmpeg                       │ │
                          │  │  ├─ Playwright/Chromium          │ │
                          │  │  └─ Python 3.11                  │ │
                          │  └──────────────────────────────────┘ │
                          │                                        │
                          │  URL: https://xxxxx.awsapprunner.com   │
                          └────────┬───────────────┬───────────────┘
                                   │               │
                    ┌──────────────┘               └──────────────┐
                    ▼                                             ▼
    ┌────────────────────────────┐              ┌─────────────────────────────┐
    │   AWS Secrets Manager      │              │   Amazon RDS                │
    │                            │              │   (PostgreSQL)              │
    │  astro-ai/prod:            │              │                             │
    │  ├─ SECRET_KEY             │              │  Database: astro_db         │
    │  ├─ DB_PASSWORD            │              │  Instance: db.t4g.micro     │
    │  ├─ OPENAI_API_KEY         │              │  Port: 5432                 │
    │  └─ Other secrets          │              │                             │
    └────────────────────────────┘              └─────────────────────────────┘
                    ▲
                    │
                    └─────────────────────────────────────────┐
                                                              │
                    ┌─────────────────────────────────────────┘
                    │
    ┌────────────────────────────────────────────────────────────────┐
    │   AWS CloudWatch                                               │
    │                                                                │
    │  ├─ Application Logs                                           │
    │  ├─ Performance Metrics                                        │
    │  ├─ Error Tracking                                             │
    │  └─ Cost Monitoring                                            │
    └────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Deployment Flow

### Option 1: Manual Deployment (Current)

```
┌──────────────┐
│ 1. Local     │
│    Changes   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 2. Docker    │
│    Build     │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 3. ECR Login │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 4. Tag &     │
│    Push      │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 5. Trigger   │
│    Deploy    │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ 6. App       │
│    Runner    │
│    Deploys   │
└──────────────┘
```

**Command:**
```bash
./deploy-to-apprunner.sh
```

---

### Option 2: Automated CI/CD (GitHub Actions)

```
┌──────────────┐
│ 1. Push to   │
│    GitHub    │
│    (main)    │
└──────┬───────┘
       │
       ▼
┌────────────────────────────────────────┐
│ 2. GitHub Actions Workflow             │
│                                        │
│    ┌──────────────┐                   │
│    │ Checkout     │                   │
│    └──────┬───────┘                   │
│           │                           │
│           ▼                           │
│    ┌──────────────┐                   │
│    │ AWS Auth     │                   │
│    └──────┬───────┘                   │
│           │                           │
│           ▼                           │
│    ┌──────────────┐                   │
│    │ Docker Build │                   │
│    └──────┬───────┘                   │
│           │                           │
│           ▼                           │
│    ┌──────────────┐                   │
│    │ Push to ECR  │                   │
│    └──────┬───────┘                   │
│           │                           │
│           ▼                           │
│    ┌──────────────┐                   │
│    │ Deploy       │                   │
│    └──────────────┘                   │
│                                        │
└────────────────────────────────────────┘
       │
       ▼
┌──────────────┐
│ 3. App       │
│    Runner    │
│    Auto      │
│    Updates   │
└──────────────┘
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    AWS Account                              │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  IAM Roles & Permissions                             │  │
│  │                                                      │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │ AppRunnerECRAccessRole                      │    │  │
│  │  │ • Pull images from ECR                      │    │  │
│  │  │ • Attached Policy:                          │    │  │
│  │  │   AWSAppRunnerServicePolicyForECRAccess     │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │ AppRunnerInstanceRole (Optional)            │    │  │
│  │  │ • Access Secrets Manager                    │    │  │
│  │  │ • Access SSM Parameter Store                │    │  │
│  │  │ • Custom permissions for S3, etc.           │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Network Security                                    │  │
│  │                                                      │  │
│  │  ┌─────────────────────────────────────────────┐    │  │
│  │  │ App Runner VPC Connector (Optional)         │    │  │
│  │  │ • Private subnet access                     │    │  │
│  │  │ • RDS in private subnet                     │    │  │
│  │  │ • VPC Security Groups                       │    │  │
│  │  └─────────────────────────────────────────────┘    │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Secrets Management                                  │  │
│  │                                                      │  │
│  │  • Secrets Manager: Encrypted at rest             │  │
│  │  • IAM-based access control                       │  │
│  │  • Automatic rotation (optional)                  │  │
│  │  • Audit logging in CloudTrail                    │  │
│  │                                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 Container Architecture

```
┌─────────────────────────────────────────────────────────┐
│  Docker Container (astro-ai:latest)                     │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Base Image: python:3.11                           │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ System Dependencies                               │ │
│  │ ├─ ffmpeg (audio processing)                      │ │
│  │ ├─ postgresql-client                              │ │
│  │ ├─ gcc, build-essential                           │ │
│  │ ├─ libgl1, libglib2.0-0                           │ │
│  │ └─ Chromium (via Playwright)                      │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Python Dependencies                               │ │
│  │ ├─ Django 5.2.1                                   │ │
│  │ ├─ PyTorch (CPU) 2.6.0                            │ │
│  │ ├─ Transformers, LangChain                        │ │
│  │ ├─ Celery, Redis                                  │ │
│  │ ├─ Playwright                                     │ │
│  │ └─ ~190 other packages                            │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Application Code                                  │ │
│  │ ├─ /app/My_AI_Guruji/ (Django project)           │ │
│  │ ├─ /app/langchain_agent/ (AI services)           │ │
│  │ ├─ /app/telegram_interface/                      │ │
│  │ ├─ /app/whatsapp_interface/                      │ │
│  │ └─ /app/ai_services/                             │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Process Management                                │ │
│  │ ┌──────────────────────────────────────────────┐  │ │
│  │ │ Gunicorn (WSGI Server)                       │  │ │
│  │ │ ├─ Worker 1 (Thread pool: 4)                 │  │ │
│  │ │ └─ Worker 2 (Thread pool: 4)                 │  │ │
│  │ │    ↓                                          │  │ │
│  │ │ Django Application                            │  │ │
│  │ │ ├─ Request handling                           │  │ │
│  │ │ ├─ AI processing (LangChain)                  │  │ │
│  │ │ ├─ TTS/STT services                           │  │ │
│  │ │ └─ PDF generation (Playwright)                │  │ │
│  │ └──────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ Exposed Ports                                     │ │
│  │ └─ 8000 (HTTP)                                    │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Application Lifecycle

### Build Phase (One-time)
```
┌──────────────────┐
│ Docker Build     │
│ ────────────     │
│ 1. Base image    │
│ 2. System deps   │
│ 3. Python deps   │
│ 4. Playwright    │
│ 5. Copy code     │
│ 6. Setup dirs    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Push to ECR      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Image stored     │
└──────────────────┘
```

### Runtime Phase (Every deployment)
```
┌──────────────────┐
│ Pull from ECR    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Pre-run commands │
│ ──────────────   │
│ • Migrate DB     │
│ • Create dirs    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Start Gunicorn   │
│ ──────────────   │
│ • 2 workers      │
│ • 4 threads each │
│ • Port 8000      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Health checks    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Service ready    │
│ Accept traffic   │
└──────────────────┘
```

---

## 📊 Scaling Behavior

```
Low Traffic          Medium Traffic        High Traffic
────────────         ──────────────        ────────────

┌─────────┐          ┌─────────┐          ┌─────────┐
│Instance │          │Instance │          │Instance │
│   #1    │          │   #1    │          │   #1    │
└─────────┘          ├─────────┤          ├─────────┤
                     │Instance │          │Instance │
Min: 1 instance      │   #2    │          │   #2    │
                     └─────────┘          ├─────────┤
                                          │Instance │
                     Auto-scale           │   #3    │
                     based on:            ├─────────┤
                     • CPU usage          │Instance │
                     • Request rate       │   #4    │
                                          └─────────┘

                                          Max: 25 instances
```

**Scaling Configuration:**
- Min instances: 1
- Max instances: 25 (default)
- Max concurrency: 100 requests/instance
- Scale-up trigger: CPU > 70% or Request queue
- Scale-down: Gradual, based on idle time

---

## 💾 Data Flow

```
User Request
    │
    ▼
┌─────────────────────┐
│ AWS App Runner      │
│ (Load Balancer)     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ Django Application  │
│ ─────────────────   │
│ • Receive request   │
│ • Process logic     │
│ • Call AI services  │
└──────────┬──────────┘
           │
           ├─────────────────┐
           │                 │
           ▼                 ▼
┌──────────────────┐  ┌──────────────────┐
│ PostgreSQL DB    │  │ External APIs    │
│ ──────────────   │  │ ──────────────   │
│ • User data      │  │ • OpenAI         │
│ • Session data   │  │ • Deepgram       │
│ • AI configs     │  │ • Other services │
└──────────────────┘  └──────────────────┘
           │
           ▼
┌─────────────────────┐
│ Response to User    │
└─────────────────────┘
```

---

## 🔍 Monitoring & Observability

```
┌────────────────────────────────────────────────┐
│              AWS CloudWatch                    │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │ Application Logs                         │ │
│  │ ├─ Access logs (Gunicorn)                │ │
│  │ ├─ Error logs                            │ │
│  │ ├─ Django debug logs                     │ │
│  │ └─ Custom application logs               │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │ Metrics                                  │ │
│  │ ├─ Request count                         │ │
│  │ ├─ Response time (p50, p95, p99)         │ │
│  │ ├─ Error rate                            │ │
│  │ ├─ CPU utilization                       │ │
│  │ ├─ Memory usage                          │ │
│  │ └─ Active connections                    │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │ Alarms (Optional)                        │ │
│  │ ├─ High error rate → SNS notification    │ │
│  │ ├─ High latency → SNS notification       │ │
│  │ └─ Cost threshold → Email alert          │ │
│  └──────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘
```

---

## 📝 File Structure in Container

```
/app/                                    (WORKDIR)
├── manage.py                            (Django entry point)
├── requirements.txt                     (Python dependencies)
├── My_AI_Guruji/                        (Django project)
│   ├── __init__.py
│   ├── settings.py                      (Configuration)
│   ├── urls.py                          (URL routing)
│   └── wsgi.py                          (WSGI entry)
│
├── langchain_agent/                     (AI services)
│   ├── models.py
│   ├── views.py
│   └── utils.py
│
├── telegram_interface/                  (Telegram bot)
├── whatsapp_interface/                  (WhatsApp bot)
├── ai_services/                         (TTS/STT)
│
├── static/                              (Static files)
├── staticfiles/                         (Collected static)
│
├── notifications/                       (Background tasks)
│   ├── logs/                            (Created at runtime)
│   ├── daily/
│   └── weekly/
│
└── /tmp/                                (Temporary files)
    └── playwright/                      (Browser cache)
```

---

## 🎯 Key Differences: Managed Runtime vs Docker

### Managed Runtime (apprunner.yaml)
```
GitHub Repo
    │
    ▼
App Runner Build Service
    │
    ├─ Pre-build commands
    ├─ Build commands (pip install)
    ├─ Post-build commands
    │
    ▼
Python Runtime Environment (Limited)
    │
    └─ Run command (Gunicorn)
```

**Limitations:**
- ❌ No system package installation
- ❌ Read-only filesystem
- ❌ No ffmpeg, chromium, etc.

---

### Docker-Based (Recommended)
```
Local/CI
    │
    ▼
Docker Build
    │
    ├─ Base image
    ├─ System packages (apt-get)
    ├─ Python packages
    ├─ Application code
    │
    ▼
ECR Repository
    │
    ▼
App Runner pulls and runs
```

**Benefits:**
- ✅ Full system control
- ✅ Custom dependencies
- ✅ Reproducible builds

---

## 🚀 Summary

**Recommended Architecture:**
- ✅ Docker-based deployment
- ✅ Amazon ECR for image storage
- ✅ AWS App Runner for hosting
- ✅ Amazon RDS for database
- ✅ AWS Secrets Manager for secrets
- ✅ CloudWatch for monitoring

**Source Directory:**
- ✅ Root directory (`/`)
- ✅ No configuration needed

**Deployment:**
- ✅ Use `./deploy-to-apprunner.sh`
- ✅ Or set up GitHub Actions CI/CD

